version: '3.8'
services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"

  # cd-java-exporter:
  #   build: ../exporters/cd-java-exporter
  #   environment:
  #     CDNODE: "10.0.0.1"
  #     CDNODEAPIPORT: "1363"
  #     CDNODEUSERID: "cdadmin"
  #     CDNODEPASSWORD: "********"
  #     METRICS_PORT: "9402"
  #   ports:
  #     - "9402:9402"
  #   depends_on:
  #     - prometheus
